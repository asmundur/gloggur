# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "gloggur"

[setup]
script = '''
set -euo pipefail

# Ensure Python exists
command -v python3 >/dev/null

# Bootstrap dev env if missing
if [ ! -d .venv ]; then
  python3 -m venv .venv
fi

. .venv/bin/activate
python -m pip install -U pip

# Install project in editable mode (fast dev loop)
python -m pip install -e ".[dev]"

# Make repo-local commands discoverable
export PATH="$PWD:$PATH"
'''
